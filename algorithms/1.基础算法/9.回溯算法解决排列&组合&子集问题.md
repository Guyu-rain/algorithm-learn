# 回溯算法解决所有排列/组合/子集问题
先回顾以下回溯算法：
```python
# 传入已经选择过的 和 备选的
def backtrack(路径, 选择列表):
    if 路径满足条件:
        result.append(路径)
        return
    
    for 选择 in 选择列表:
        # 做选择
        选择列表中去掉这个选择
        路径.append(选择)
        backtrack(路径, 选择列表)
        路径.remove(选择)
        选择列表中重新加入这个选择
```
从备选集合和可选两个方面来看，排列、组合、子集问题分成三种：
1. 元素无重复 + 不可复选
2. 元素可重复 + 不可复选
3. 元素不重复 + 可以复选
- 元素可重复 + 可以复选 这种没有意义。都可以复选了，重复与否都没有意义了。

不管是这三种哪一种的排列/组合/子集问题（一共3x3=9种），或者其他的变体，本质都是穷举所有解，而且这个解结构必然**呈现树状结构**，所以需要合理使用回溯框架 + 题目限制，来解决所有问题。这个树结构可以分成：
- 组合 / 子集树（组合和子集问题是等价的）
- 排列树

## 子集
子集最重要的是不出现重复元素：
我们通过**保证元素之间的相对顺序不变**防止出现重复的子集。
子集树有如下特点：
**如果将根节点视作第 0 层，将每个节点和根节点之间的树枝上的元素作为该节点的值，那么第 n 层的所有节点就是大小为 n 的所有子集。**
```text
                  [ ]
            /      |      \
          [1]     [2]     [3]
         /   \     |
      [1,2] [1,3] [2,3]
        |
     [1,2,3]
```
注意，这颗树的树枝上表示元素的选择，从左向右保持着相对顺序不变。
```C++
// res 就是答案
vector<vector<int>> res;
// 记录回溯算法的递归路径
vector<int> track;

void backtrack(vector<int>& nums, int start) {
    // 前序位置，每个节点都是一个子集
    res.push_back(track);

    // 回溯算法标准框架
    // 需要保证元素之间的相互顺序
    for (int i = start; i < nums.size(); i++) {
        // 做选择
        track.push_back(nums[i]);
        // 通过参数 start 控制树的遍历，也就是控制元素之间的相对关系
        backtrack(nums, i + 1);
        // 撤销选择
        track.pop_back();
    }
}
```

## 组合
组合问题可以很轻松的转化成子集问题：**大小为`k`的组合 等价于 所有大小为`k`的子集**，也就是要第`k`层子集树上的结果。
```C++
vector<vector<int>> res;
// 记录回溯算法的递归路径
vector<int> track;

void backtrack(vector<int>& nums, int start, int k) {
    // 第 k 层
    if (k == track.size()) {
        res.push_back(track);
        return;
    }

    for (int i = start; i < nums.size(); i++) {
        // 做选择
        track.push_back(nums[i]);
        // 进入下一个节点
        backtrack(nums, i + 1, k);
        // 撤销选择
        track.pop_back();
    }
}
```
可以做一个简单的剪枝，当剩余元素`nums.size()-i` < `k-track.size()` 时，必然不可能产生一个大小为 k 的集合 `track`，因此直接`return`即可

## 排列
排列组成的树是**排列树**。排列数的特点是不需要限制元素之间的相对位置。
需要限制的是，选过元素的不能再选，需要通过一个`used`数组来标记哪些元素已经备选过。
```C++
// 存储所有排列结果的列表
vector<vector<int>> res;
// 记录回溯算法的递归路径
vector<int> track;
// track 中的元素会被标记为 true
vector<bool> used;

void backtrack(vector<int>& nums) {
    if (track.size() == nums.size()) {
        res.push_back(track);
        return;
    }

    for (int i = 0; i < nums.size(); i++>) {
        // 先判断是否可以选，然后做选择
        if (used[i]) {
            continue;
        }
        // 做选择
        used[i] = true;
        track.push_back(nums[i]);
        // 进入下一层回溯树
        backtrack(nums);
        // 撤销选择
        track.pop_back();
        used[i] = false;
    }
}
```
如果要 `k` 个元素组成的，那么添加一个参数，然后改一下判断条件即可：
```C++
void backtrack(vector<int>& nums, int k) {
    if (track.size() == k) {
        res.push_back(track);
        return;
    }

    ...
}
```